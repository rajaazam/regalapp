import 'package:flutter/material.dart';

class Splachscreendriver extends StatelessWidget {
  const Splachscreendriver({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('wellcom to driver '),
      ),
    );
  }
}
